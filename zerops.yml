zerops:
  - setup: app
    build:
      base: rust@stable
      buildCommands:
        - cargo build --release
      deployFiles:
        - target/release/rust-web-tcg
    run:
      start: ./rust-web-tcg